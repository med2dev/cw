// P: num, a number, appears to be only integers

// R: a string representation of that number

// E:
numberToString('12') // 12

// this exists as a builtin
function numberToString(num){
    return String(num)
}

// debug challenge

// P: two numbers
// R: the first divided by the second


// E: 
solve(4, 1) // 4
solve(5, 0) // Infinity

// P: typo here is double /, which is the syntax for a comment
const solve = (x, y) => x // y
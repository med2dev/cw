// P: two strings, obj and feature
// R: a string, obj and feature concatenated with 'are, as in 

// E:
templeStrings('Animals', 'Good') // 'Animals are Good'

function templeStrings(obj, feature){

}
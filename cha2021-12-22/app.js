// P: an integer and a string
// R: the string, concatenated n times

// E:
repeatStr(6, "I") // "IIIIII"


// this already exists as a string method
function repeatStr(n, s){
    return s.repeat(n)
}